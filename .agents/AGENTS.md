# Rules

- Always prioritize using the `ui-ux-pro-max` skill whenever designing or making changes to the frontend UI or UX. The skill is installed in the `.agents/skills` directory and is automatically discovered.
